import crypto from 'node:crypto';
export const RELATIONAL_PRESENTATION_BINDING_V1_IDENTITY=crypto.createHash('sha256').update('GSI_V1_GENERIC_ROOT_LOCAL_PRESENTATION_BINDING_MAX_THEN_BOUNDED_COMPOSITION_R1').digest('hex').toUpperCase();
const clamp01=value=>Math.max(0,Math.min(1,value));
function requireSelected(selected){if(!selected||!Array.isArray(selected.rootChannels)||!Number.isFinite(selected.structuralContribution))throw new TypeError('C2 relational consumer output required')}
function rootResponse(root){if(!root||typeof root.rootId!=='string'||!Array.isArray(root.primaryLobeChannels))throw new TypeError('generic root channel required');const energy=clamp01(root.premultipliedContribution),coreContribution=energy*clamp01(root.coreChannel),lobeContributions=root.primaryLobeChannels.map(value=>energy*clamp01(value)),lobeContribution=lobeContributions.length?Math.max(...lobeContributions):0;return Object.freeze({rootId:root.rootId,coreContribution,lobeContributions:Object.freeze(lobeContributions),lobeContribution,rootLocalContribution:Math.max(coreContribution,lobeContribution)})}
export function bindRootLocalPresentation(selected){requireSelected(selected);const rootResponses=selected.rootChannels.map(rootResponse),aggregateContribution=rootResponses.length?Math.max(...rootResponses.map(root=>root.rootLocalContribution)):0;return Object.freeze({candidate:'I2_RELATIONAL_ROOT_CHANNEL_BINDING_WITH_ROOT_LOCAL_PRESENTATION_COMPOSITION',aggregateContribution:clamp01(aggregateContribution),rootResponses:Object.freeze(rootResponses),rootChannelsConsumed:true,rootOwnershipRetained:true,lobeIdentityRetained:`${rootResponses.reduce((sum,root)=>sum+root.lobeContributions.length,0)}/${selected.lobeIdentitySurvival}`,scalarCollapse:false,collapseOperation:null,composition:'ROOT_LOCAL_CORE_LOBE_MAX_THEN_INTER_ROOT_MAX'})}
