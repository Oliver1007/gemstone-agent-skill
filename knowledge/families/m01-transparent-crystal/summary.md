# M01 Transparent Crystal — Runtime Summary

M01 describes a transparent host whose identity depends on physical interfaces, optical path length, absorption/transmission relationships, scale, geometry, and the identity-relevant Optical Environment.

Use the order:

`Geometry → clean physical foundation → Core identity → Reference calibration → optional structure → Presentation`

M01 is not a shader preset. Do not import exact Smoky Quartz or Citrine values as Family defaults.

For ordinary classification, stop after this summary when it is decision-sufficient. Load `qa.yaml` for QA decisions and `failures.yaml` only for a relevant diagnosed symptom cluster.

The structured manifest and Knowledge records are authoritative; this summary is a non-independent loading aid.
